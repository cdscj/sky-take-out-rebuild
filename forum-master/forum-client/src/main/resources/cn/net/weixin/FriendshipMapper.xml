<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.net.wenxin.mapper.FriendshipMapper">
    <select id="selectFriendship" resultType="cn.net.wenxin.client.entity.Friendship">
        SELECT id, user_id as userId, friend_id as friendId, status, created_at as createdAt
        FROM friendship
        WHERE (user_id = #{userId} AND friend_id = #{friendId})
        OR (user_id = #{friendId} AND friend_id = #{userId})
    </select>
</mapper>